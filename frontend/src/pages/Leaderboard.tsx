import { Stack, Typography } from "@mui/material";

export default function Leaderboard() {
    return (
        <Stack>
            <Stack sx={{ alignItems: 'center', alignText: 'center' }}>
                <Typography variant="h2" color="white">Leaderboard</Typography>
            </Stack>
        </Stack>
    );
}